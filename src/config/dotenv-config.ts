import 'dotenv/config';

export const {
  ACCEPTED_ORIGIN,
  PORT,
  PG_DB,
  PG_HOST,
  PG_PORT,
  PG_PASSWORD,
  PG_USER,
  SECRETKEY,
  NODE_ENV,
  EMAIL_ADDRESS,
  SENDGRID_API_KEY,
} = process.env;
